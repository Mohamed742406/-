# منصة محمد القصبي لإدارة المواصفات الهندسية

منصة ويب متكاملة لإدارة ملفات المواصفات الهندسية والترجمات، مع دعم المعايير الدولية (ASTM, ACI, BS) وتقييم المستخدمين.

## المميزات

- ✅ واجهة عربية RTL احترافية
- ✅ إدارة المعايير والملفات الهندسية
- ✅ نظام بحث متقدم
- ✅ تقييم المنصة بالنجوم
- ✅ لوحة تحكم للأدمن
- ✅ رفع ملفات حتى 150MB
- ✅ قاعدة بيانات PostgreSQL

## المتطلبات

- Node.js v14+
- PostgreSQL
- npm أو yarn

## التثبيت المحلي

```bash
# 1. استنساخ المشروع أو فك ضغط الملف
unzip engineering-specs-platform.zip
cd engineering-specs-platform

# 2. تثبيت الحزم
npm install

# 3. إنشاء ملف .env
cp .env.example .env

# 4. تعديل متغيرات البيئة
# قم بتعديل ملف .env وأضف DATABASE_URL الخاص بـ PostgreSQL

# 5. تشغيل الخادم
npm start
```

## النشر على Render

### الخطوة 1: إعداد GitHub

1. قم بإنشاء حساب على [GitHub](https://github.com)
2. أنشئ مستودع جديد باسم `engineering-specs-platform`
3. قم برفع جميع ملفات المشروع:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/engineering-specs-platform.git
   git push -u origin main
   ```

### الخطوة 2: إنشاء قاعدة بيانات PostgreSQL على Render

1. اذهب إلى [Render](https://render.com)
2. سجل دخول أو أنشئ حساب جديد
3. من لوحة التحكم، اختر **"New +"** ثم **"PostgreSQL"**
4. ملأ البيانات:
   - **Name:** `engineering-specs-db`
   - **Database:** `engineering_specs_platform`
   - **User:** `postgres`
5. اضغط **"Create Database"**
6. انتظر حتى يتم إنشاء قاعدة البيانات (قد يستغرق دقيقة أو دقيقتين)
7. انسخ **Internal Database URL** (ستحتاجها لاحقاً)

### الخطوة 3: إنشاء Web Service على Render

1. من لوحة التحكم، اختر **"New +"** ثم **"Web Service"**
2. اختر **"Connect a repository"** وربط مستودع GitHub الخاص بك
3. ملأ البيانات:
   - **Name:** `engineering-specs-platform`
   - **Runtime:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
4. اضغط **"Advanced"** وأضف متغيرات البيئة:
   - **DATABASE_URL:** (الرابط الذي نسخته من قاعدة البيانات)
   - **ADMIN_PASSWORD:** `elkasaby2025`
   - **PORT:** `4000`
   - **NODE_ENV:** `production`
5. اضغط **"Create Web Service"**
6. انتظر حتى ينتهي البناء والنشر (قد يستغرق 3-5 دقائق)

### الخطوة 4: الوصول إلى الموقع

بعد انتهاء النشر، ستحصل على رابط دائم مثل:
```
https://engineering-specs-platform-xxxx.onrender.com
```

## استخدام المنصة

### تسجيل الدخول كأدمن

1. اضغط على زر **"تسجيل الدخول - أدمن"** في الهيدر
2. أدخل كلمة السر: `elkasaby2025`
3. بعد تسجيل الدخول، سيظهر زر **"رفع ملف جديد"**

### رفع ملف جديد

1. اضغط على زر **"رفع ملف جديد"**
2. اختر المعيار (ASTM, ACI, BS, أو أكواد أخرى)
3. أدخل عنوان الملف والوصف
4. اختر الملف (حتى 150MB)
5. اضغط **"رفع الملف"**

### البحث عن ملفات

استخدم صندوق البحث في قسم "البحث عن الملفات" للبحث عن ملفات محددة.

### تقييم المنصة

اذهب إلى قسم "قيّم المنصة" واختر عدد النجوم وأضف تعليقك.

## هيكل المشروع

```
engineering-specs-platform/
├── server.js              # ملف الخادم الرئيسي
├── package.json           # قائمة الحزم
├── .env                   # متغيرات البيئة
├── .gitignore             # استبعاد الملفات من Git
├── public/
│   ├── index.html         # الصفحة الرئيسية
│   ├── style.css          # أنماط CSS
│   └── main.js            # منطق JavaScript
├── uploads/               # مجلد الملفات المرفوعة
└── README.md              # هذا الملف
```

## متغيرات البيئة

| المتغير | الوصف | المثال |
|---------|-------|--------|
| `DATABASE_URL` | رابط قاعدة البيانات PostgreSQL | `postgresql://user:pass@host:port/db` |
| `ADMIN_PASSWORD` | كلمة سر الأدمن | `elkasaby2025` |
| `PORT` | منفذ الخادم | `4000` |
| `NODE_ENV` | بيئة التشغيل | `production` |

## الـ API Endpoints

### المعايير
- `GET /api/standards` - الحصول على جميع المعايير
- `GET /api/statistics` - الحصول على الإحصائيات
- `GET /api/standards/:id/files` - الحصول على ملفات معيار

### الملفات
- `GET /api/files/:id/download` - تحميل ملف
- `POST /api/files/upload` - رفع ملف جديد (أدمن)
- `DELETE /api/files/:id` - حذف ملف (أدمن)

### البحث والتقييم
- `GET /api/search?query=` - البحث عن ملفات
- `POST /api/ratings` - إضافة تقييم
- `GET /api/ratings/summary` - ملخص التقييمات

### الأدمن
- `POST /api/admin/login` - تسجيل دخول الأدمن
- `GET /api/admin/status` - التحقق من حالة الأدمن

## استكشاف الأخطاء

### الموقع لا يعمل بعد النشر

1. تحقق من أن `DATABASE_URL` صحيح في متغيرات البيئة
2. تأكد من أن قاعدة البيانات PostgreSQL تعمل بشكل صحيح
3. تحقق من سجلات الخادم في لوحة تحكم Render

### الملفات المرفوعة لا تظهر

تأكد من أن مجلد `uploads/` موجود وله صلاحيات الكتابة.

### خطأ في الاتصال بقاعدة البيانات

1. تحقق من أن `DATABASE_URL` يحتوي على بيانات الاتصال الصحيحة
2. تأكد من أن قاعدة البيانات PostgreSQL تعمل
3. جرب إعادة تشغيل الخادم

## الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل عبر البريد الإلكتروني أو إنشاء issue على GitHub.

## الترخيص

هذا المشروع مرخص تحت رخصة MIT.

---

**تم إنشاء هذا المشروع بواسطة Manus AI**
